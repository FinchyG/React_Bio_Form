import React from 'react';
import { Title } from './title';

export default class Bio_Form extends React.Component {
    
    render() {

        return (
            <div id="form">
                <Title />
            </div>

        )
    }
}